# 2025-11-21

## 🧠 오늘 배운 것
- Django Nested Serializer
    - 모델 간의 관계(FK, OneToOne, ManyToMany)를 직렬화할 때,
    단순히 id만 내려주는 것이 아니라 관련 객체의 정보까지 포함해서 응답하는 방식

### 기본 Serializer vs. Nested Serializer
1. 기본 Serializer
    ```python
    class NoticeSerializer(serializer.ModelSerializer):
        class Meta:
            model = Notice
            fields = '__all__'
    ```
    ```json
    {
        "id": 1,
        "study": 3,
        "author": 7,
        "title": "공지"
    }
    ```
2. Nested Serializer
    ```python
    class UserSrializer(serializers.ModelSerializer):
        class Meta:
            model = User
            fields = ('id', 'username')

    class NoticeSerializer(serializers.ModelSerializer):
        author = UserSerializer(read_only=True)

        class Meta:
            model = Notice
            fields = '__all__'
    ```
    ```json
    {
        "id": 1,
        "study": 3,
        "author": {
            "id": 7,
            "username": "라지트"
        },
        "title": "공지"
    }

### Nested Serializer가 필요한 이유
- 프론트에서 id만으로는 정보가 부족할 때
    - username, profile image 등 사용자 상세 정보가 필요할 수 있음
- API 응답 구조의 명확성 향상
    - 연관된 객체를 직렬화해 응답하면, 별도의 추가 요청 없이 데이터 참조 가능
- 유지보수성 향상
    - 각 모델의 serializer가 독립적이며, 재사용하기 좋음

### 주의할 점
- Nested Serializer는 기본적으로 읽기 전용(`read_only=True`)으로 쓰는게 안전함
    - Nested된 객체를 한 번에 생성/수정하려면 별도 처리 로직이 필요
- 쿼리 수 증가 가능
    - Nested Serailizer에 depth가 깊으면 쿼리가 여러 번 발생할 수 있음
    - `selected_related`, `prefetch_related`로 최적화 필요